{
  "dependencies": {
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@electron/packager": "^20.0.2",
    "electron": "latest"
  },
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "npx @electron/packager . TRY2 --platform=win32 --arch=x64 --out=dist"
  }
}